# 🚀 Quick Start Guide

## ✅ System Status

Your Library Management System is now running!

- **Backend API**: http://localhost:5001
- **Frontend UI**: http://localhost:3000
- **Database**: SQLite (library.db)

## 📊 Current Data

- **Books**: 5 books in the system
- **Members**: 4 active members
- **Issues**: 6 total (4 active, 2 returned)
- **Fines Collected**: $50

## 🌐 Access the Application

Open your browser and go to:
```
http://localhost:3000
```

You'll see:
- Dashboard with statistics
- Books management
- Members management
- Issue/Return tracking

## 🧪 Test the API

```bash
# Get all books
curl http://localhost:5001/api/books

# Get all members
curl http://localhost:5001/api/members

# Get dashboard stats
curl http://localhost:5001/api/admin/stats

# Get all issues
curl http://localhost:5001/api/issues

# Get overdue books
curl http://localhost:5001/api/issues/overdue
```

## 📝 Sample Data Included

### Books
- Effective Java by Joshua Bloch
- Clean Code by Robert C. Martin
- Design Patterns by Gang of Four
- Head First Design Patterns by Eric Freeman
- The Clean Coder by Robert C. Martin

### Members
- John Doe (MEM001)
- Jane Smith (MEM002)
- Bob Johnson (MEM003)
- Alice Williams (MEM004)

## 🛠️ Development Commands

### Backend
```bash
cd backend
npm run dev    # Start with auto-reload
```

### Frontend
```bash
cd frontend
npm run dev    # Start development server
npm run build  # Build for production
```

## 📁 Project Structure

```
library-management-system/
├── backend/          # Express API server
│   ├── src/
│   │   ├── controllers/  # Business logic
│   │   ├── routes/       # API endpoints
│   │   ├── config/       # Database config
│   │   └── middleware/   # Auth & validation
│   └── library.db    # SQLite database
├── frontend/         # React UI
│   └── src/
│       ├── pages/    # Dashboard, Books, Members, Issues
│       └── services/ # API client
└── database/         # SQL schemas
```

## 🔧 Configuration

Backend configuration is in `backend/.env`:
```env
PORT=5001
USE_SQLITE=true
JWT_SECRET=<auto-generated>
```

## 🎯 Next Steps

1. Explore the UI at http://localhost:3000
2. Try adding a new book or member
3. Issue a book to a member
4. Return a book and see fine calculation
5. Check the dashboard for updated statistics

## 🐛 Troubleshooting

**Backend not starting?**
- Check if port 5001 is available
- Verify .env file exists in backend/

**Frontend not loading?**
- Check if port 3000 is available
- Ensure backend is running first

**Database errors?**
- Delete library.db and restart backend
- It will recreate the database automatically

## 📚 Features to Explore

- ✅ Real-time book availability tracking
- ✅ Automatic fine calculation ($5/day)
- ✅ Search and filter books
- ✅ Member borrowing history
- ✅ Overdue book tracking
- ✅ Admin dashboard with statistics

Enjoy your Library Management System! 🎉
